version: "3.8"

services:
  logistiek-api:
    build: .
    image: logistiek-poc:latest
    ports:
      - "8080:8080"
    environment:
      - PORT=8080

  # Bij lokaal testen werkte dit niet
  # deploy:
  #   replicas: 2
